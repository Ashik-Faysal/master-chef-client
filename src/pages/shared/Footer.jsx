import React from 'react';
import { Container } from 'react-bootstrap';

const Footer = () => {
    return (
        <Container>
            <small className='text-center'>Thios is footer</small>
        </Container>
    );
};

export default Footer;